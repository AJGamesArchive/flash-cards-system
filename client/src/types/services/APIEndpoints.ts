/**
 * Type to define all server API GET endpoints
 */
export type APIGetEndpoint = 'Pending';

/**
 * Type to define all server API POST endpoints
 */
export type APIPostEndpoint = 'Pending';

/**
 * Type to define all server API PUT endpoints
 */
export type APIPutEndpoint = 'Pending';

/**
 * Type to define all server API PATCH endpoints
 */
export type APIPatchEndpoint = 'Pending';

/**
 * Type to define all server API DELETE endpoints
 */
export type APIDeleteEndpoint = 'Pending';