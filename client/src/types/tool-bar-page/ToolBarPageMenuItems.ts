/**
 * Type to define the data that powers the tool bar page menu items
 */
type ToolBarPageMenuItems = {
  itemName: string;
  icon: string;
  onCLick: () => void;
};

export default ToolBarPageMenuItems;