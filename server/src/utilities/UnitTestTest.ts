/**
 * TODO Remove file later
 * Function to test that Jest unit testing is setup correctly
 * Function to format a given first name and last name
 * @param firstName First Name
 * @param lastName Last Name
 * @returns First name and last name formatted together as one string
 */
export default function formatName(
  firstName: string,
  lastName: string
): string {
  return `${firstName} ${lastName}`;
}
